exports.run = (client, message, args) => {
    message.channel.send("Hello, Trainer! The **Traveler Map** is a great way for Trainers to connect with other Pokemon GO communities. See the map below to get connected. Also using the command `!oak nestatlas` to discover nests.").catch(console.error);
}
